// Para testar: yarn test -- first_occurrence.test.ts

// findFirstOccurrence recebe uma string e uma letra, e deve retornar
// a primeira posição que essa letra aparece na string, considerando espaços em branco.
// Retorna -1 caso a letra não exista na string.
/*
    Exemplo:
        findFirstOccurrence("abcd", "d") => 3
        findFirstOccurrence("a b c d", "d") => 6
        findFirstOccurrence("abcd", "x") => -1
*/
function findFirstOccurrence(str: string, letter: string): number {
    return 0;
}

export default findFirstOccurrence;
