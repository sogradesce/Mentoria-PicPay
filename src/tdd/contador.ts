function contador(frase: any, letraProcurada: any) {
    var fraseEfeito =
        "A melhor forma para começar bem o dia, é desejando o bem para outra pessoa.";

    for (var i = 0; i < frase.length; i++) {
        if (letraProcurada[i] == fraseEfeito) {
        }
    }
}

export default contador;
