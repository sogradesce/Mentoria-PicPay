function paridade(numeros: number[]): [number[], number[]] {
    let numerosPares = [2, 4];
    let numerosImpares = [1, 3, 5];

    return [numerosPares, numerosImpares];
}

export default paridade;
